# ─────────────────────────────────────────────────────────────────────────────
# Autostart — servicios que requieren el compositor activo
# ─────────────────────────────────────────────────────────────────────────────
# Nota: wayvnc y swww-daemon se gestionan vía systemd (NixOS modules):
#   - wayvnc:     systemd.user.services.wayvnc  (hyprland-desktop.nix)
#   - swww-daemon: systemd.user.services.swww-daemon (wallpaper.nix)
#   - wallpaper:  systemd.user.services.wallpaper-apply (wallpaper.nix)
# ─────────────────────────────────────────────────────────────────────────────

# Barra de estado — corre en el contexto del compositor
exec-once = waybar
